---
menu: Usage
---

# Boolean Operators

You can use familiar JS boolean operators and brackets such as `!`, `&&`, `||`, `(`, or `)` as well as the shorter `&` and `|`:

```js
p`!( null || undefined )`;
p`!( null | undefined )`;
p`number && > 6`;
p`number & > 6`;
```
